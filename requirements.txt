click>=8.1.0
requests>=2.31.0
PyYAML>=6.0
rich>=13.0.0
urllib3>=2.0.0
pydantic>=2.0.0

# API / Docker services
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
redis>=5.0.0
psycopg2-binary>=2.9.9
PyNaCl>=1.5.0

# Agent orchestration (LangGraph PEV; optional ADK — pip install "ado2gh[agent]")
langchain>=0.2
langchain-core>=0.2
langgraph>=0.2
langgraph-checkpoint-sqlite>=2.0
# For ADO2GH_STORAGE_BACKEND=postgres use `pip install -e ".[postgres]"` — AsyncPostgresSaver
# needs both langgraph-checkpoint-postgres and psycopg v3, which psycopg2-binary above does not provide.
langchain-openai>=0.1
langchain-anthropic>=0.1
# google-adk>=2.0  # optional: ADK App/Runner tooling (Python 3.10+)

# Serverless production (optional)
# boto3>=1.34.0

# Dev / test
pytest>=8.0.0
pytest-asyncio>=0.24.0

# Optional: GitHub App authentication
# cryptography>=41.0.7
# PyJWT>=2.8.0
