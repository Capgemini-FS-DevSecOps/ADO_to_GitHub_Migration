# Exception Register

Functions that keep a tag because compliance would change behaviour (FR-005).
Every row must have a matching `inventory.json` entry with
`disposition == "exception"` and a `note` pointing here; the suppressed ruff codes
must be present as `# noqa: <code>` at the definition (research R4).

**Cap (FR-005a / SC-001)**: row count must stay `≤ 2 %` of `totals.functions` in the
latest line of `inventory-history.jsonl`. Before adding a row, check the cap; if the
row would exceed it, stop cleanup of that rule and ask the operator (raise the cap /
amend the rule / accept the behaviour change) before continuing.

Schema: data-model.md § ExceptionRegisterEntry. Columns are fixed — do not add,
reorder, or rename them.

| Inventory id | Unmet rule(s) | Reason | Follow-up owner | `noqa` code(s) |
|---|---|---|---|---|
| `ado2gh/models.py::ExecutionMode.from_dry_run` | `bool_flag` (FR-012) | This is the boundary converter that data-model.md § ExecutionMode and contracts/public-contract-freeze.md mandate: its parameter is the external `dry_run` boolean being converted, so the only enum that could replace it is the one it returns, and "two intent-named functions" would be the enum members themselves, scattering the conversion across the 65 boundary call sites. The review agent confirmed `bool_flag` at increment 1 (`tag-decisions.json`); the operator may overrule by deleting this row and re-deciding. | operator (feature 013) | — (walker-derived tag; no ruff code fires on a keyword-only bool) |
| `ado2gh/state/base.py::StateDBBase.upsert_migration` | `gt5_params` (7 > 5) | Row key (`wave_id`, `repo`, `scope`) plus `status` and the outcome columns `error`, `gh_migration_id`, `stats`. No existing record model holds a scope-migration outcome: the only candidate, `ScopeResult` in `ado2gh/core/scopes/base.py`, would make `state` import `core`, inverting the dependency the feature is removing (GAP-021); a new type is forbidden by T048. Increment 3, review-agent decisions in `tag-decisions.json`. | operator (feature 013) — revisit at T078 when the layering is fixed | `PLR0913` |
| `ado2gh/state/sqlite_db.py::SQLiteStateDB.upsert_migration` | `gt5_params` (7 > 5) | Row key (`wave_id`, `repo`, `scope`) plus `status` and the outcome columns `error`, `gh_migration_id`, `stats`. No existing record model holds a scope-migration outcome: the only candidate, `ScopeResult` in `ado2gh/core/scopes/base.py`, would make `state` import `core`, inverting the dependency the feature is removing (GAP-021); a new type is forbidden by T048. Increment 3, review-agent decisions in `tag-decisions.json`. | operator (feature 013) — revisit at T078 when the layering is fixed | `PLR0913` |
| `ado2gh/state/postgres_db.py::PostgresStateDB.upsert_migration` | `gt5_params` (7 > 5) | Row key (`wave_id`, `repo`, `scope`) plus `status` and the outcome columns `error`, `gh_migration_id`, `stats`. No existing record model holds a scope-migration outcome: the only candidate, `ScopeResult` in `ado2gh/core/scopes/base.py`, would make `state` import `core`, inverting the dependency the feature is removing (GAP-021); a new type is forbidden by T048. Increment 3, review-agent decisions in `tag-decisions.json`. | operator (feature 013) — revisit at T078 when the layering is fixed | `PLR0913` |
| `ado2gh/state/base.py::StateDBBase.upsert_pipeline_migration` | `gt5_params` (9 > 5) | Row key (`wave_id`, `meta`, `repo`) plus `status` and the transform outcome columns `workflow_file`, `error`, `warnings`, `unsupported`, `transform_stats`. `gh_org`/`gh_repo` were already grouped into the existing `RepoConfig`; the transformer returns a plain dict, so no existing record model holds a transform outcome and a new type is forbidden by T048. Increment 3. | operator (feature 013) — revisit at T078 when the layering is fixed | `PLR0913` |
| `ado2gh/state/sqlite_db.py::SQLiteStateDB.upsert_pipeline_migration` | `gt5_params` (9 > 5) | Row key (`wave_id`, `meta`, `repo`) plus `status` and the transform outcome columns `workflow_file`, `error`, `warnings`, `unsupported`, `transform_stats`. `gh_org`/`gh_repo` were already grouped into the existing `RepoConfig`; the transformer returns a plain dict, so no existing record model holds a transform outcome and a new type is forbidden by T048. Increment 3. | operator (feature 013) — revisit at T078 when the layering is fixed | `PLR0913` |
| `ado2gh/state/postgres_db.py::PostgresStateDB.upsert_pipeline_migration` | `gt5_params` (9 > 5) | Row key (`wave_id`, `meta`, `repo`) plus `status` and the transform outcome columns `workflow_file`, `error`, `warnings`, `unsupported`, `transform_stats`. `gh_org`/`gh_repo` were already grouped into the existing `RepoConfig`; the transformer returns a plain dict, so no existing record model holds a transform outcome and a new type is forbidden by T048. Increment 3. | operator (feature 013) — revisit at T078 when the layering is fixed | `PLR0913` |
| `ado2gh/state/base.py::StateDBBase.create_live_execution_approval` | `gt5_params` (10 > 5) | Approval row columns. The only existing model with the matching field set is `LiveApprovalCreateRequest` in `ado2gh/api/contracts.py`; using it would add a further `state -> api` import (GAP-021, owned by T078), which increment 3 was told not to deepen, and `PlatformUser` alone leaves six parameters. Increment 3. | operator (feature 013) — revisit at T078 when the layering is fixed | `PLR0913` |
| `ado2gh/state/sqlite_users_mixin.py::PlatformUsersMixin.create_live_execution_approval` | `gt5_params` (10 > 5) | Approval row columns. The only existing model with the matching field set is `LiveApprovalCreateRequest` in `ado2gh/api/contracts.py`; using it would add a further `state -> api` import (GAP-021, owned by T078), which increment 3 was told not to deepen, and `PlatformUser` alone leaves six parameters. Increment 3. | operator (feature 013) — revisit at T078 when the layering is fixed | `PLR0913` |
| `ado2gh/state/postgres_agentic_users_mixin.py::PostgresAgenticUsersMixin.create_live_execution_approval` | `gt5_params` (10 > 5) | Approval row columns. The only existing model with the matching field set is `LiveApprovalCreateRequest` in `ado2gh/api/contracts.py`; using it would add a further `state -> api` import (GAP-021, owned by T078), which increment 3 was told not to deepen, and `PlatformUser` alone leaves six parameters. Increment 3. | operator (feature 013) — revisit at T078 when the layering is fixed | `PLR0913` |
| `ado2gh/pipelines/extractor.py::PipelineMetadataExtractor.extract_yaml_pipeline` | `gt5_params` (7 > 5) | Three separate raw Azure DevOps payloads (the Pipelines API definition, the Build Definitions API definition and the run history) plus the project name, the pipeline YAML and the project's variable groups. No existing record model holds raw ADO responses — `PipelineMetadata` is what this function produces, not what it consumes — and a new type is forbidden by T052. Increment 5. | operator (feature 013) | `PLR0913` |
| `ado2gh/pipelines/push_workflows.py::push_repo_workflows` | `gt5_params` (8 > 5) | A GitHub client, the repository pair, the local workflow directory, four per-push options (branch, base branch, pull request title, execution mode) and the state store the readiness gate reads. The repository pair is already grouped into the existing `RepoConfig`; a client, a path and per-call options match no existing record model and a new type is forbidden by T052. Increment 5. | operator (feature 013) | `PLR0913` |
| `ado2gh/pipelines/push_workflows.py::push_workflows_for_repos` | `gt5_params` (8 > 5) | The same arguments as `push_repo_workflows`, taken once for a list of repositories instead of one. Reducing it would mean either dropping options the CLI passes through or introducing a new type, which T052 forbids. Increment 5. | operator (feature 013) | `PLR0913` |
| `ado2gh/pipelines/resolve/template_resolver.py::_expand_template_list` | `gt5_params` (6 > 5) | The list and which of `steps`/`jobs`/`stages` it is, the template fetcher, the path of the referencing file, and the recursion state `depth` and `max_depth`. The recursion state is passed the same way through `_merge_extends` and `_resolve_template_lists`, so grouping it here alone would need a new type and split the three helpers apart. Increment 5. | operator (feature 013) | `PLR0913` |
| `ado2gh/core/migration_engine.py::MigrationEngine.__init__` | `gt5_params` (9 > 5) | The two clients, the state store and the parsed `global` config the engine cannot work without, plus the execution mode and four optional run-scoping arguments (`concurrency`, `assignment_id`, `allowed_repo_keys`, `pipeline_run_id`). The clients and the config match no existing record model, and the run-scoping four are set independently by different callers — the accelerator sets `pipeline_run_id`, the cohort gate sets `allowed_repo_keys` — so no existing type holds them together and a new type is forbidden by T056. Increment 7. | operator (feature 013) — revisit at T078 when the layering is fixed | `PLR0913` |
| `ado2gh/core/scopes/branch_policies_scope.py::BranchPoliciesScopeHandler.migrate` | `unused_param` (`**kwargs`) | FR-011 requires every handler in `ado2gh/core/scopes/` to share one signature, and `MigrationEngine` dispatches through `handler.migrate(repo, ctx, **kwargs)` without knowing which handler it reached. Dropping `**kwargs` from the handlers that ignore it would break that single dispatch; the two handlers that do read it (`git_scope`, `pipelines_scope`) take `concurrency`, `pipeline_parallel` and `wave_id` from the same mapping. Increment 7. | operator (feature 013) | `ARG002` |
| `ado2gh/core/scopes/secrets_scope.py::SecretsScopeHandler.migrate` | `unused_param` (`**kwargs`) | Same shared handler signature as `BranchPoliciesScopeHandler.migrate` (FR-011); this handler writes a manifest and needs no dispatch extras. Increment 7. | operator (feature 013) | `ARG002` |
| `ado2gh/core/scopes/wiki_scope.py::WikiScopeHandler.migrate` | `unused_param` (`**kwargs`) | Same shared handler signature as `BranchPoliciesScopeHandler.migrate` (FR-011); this handler exports wiki pages and needs no dispatch extras. Increment 7. | operator (feature 013) | `ARG002` |
| `ado2gh/core/scopes/work_items_scope.py::WorkItemsScopeHandler.migrate` | `unused_param` (`**kwargs`) | Same shared handler signature as `BranchPoliciesScopeHandler.migrate` (FR-011); this handler creates issues and needs no dispatch extras. Increment 7. | operator (feature 013) | `ARG002` |
| `apps/migration-ui/src/lib/agentChat.ts::isAgentInterruptible` | `bool_flag` (FR-012) | The `flags` parameter is already an options object of four independent optional booleans (`streaming`, `messagePending`, `polling`, `formPending`) OR-ed into a single predicate — the shape FR-012 prescribes as the remediation. The walker raises `bool_flag` on the textual `boolean` inside the object type, so no signature that keeps those flags can clear the tag, and a new type is forbidden by T052. The flags are client-side in-flight state the caller owns and cannot be derived inside the function. Review agent confirmed `bool_flag` in increment 14 (`tag-decisions.json`); exception decided by the coordinator on the operator's behalf, 2026-09-08. Increment 14. | operator (feature 013) | — (walker-derived tag; ruff does not lint TypeScript) |
